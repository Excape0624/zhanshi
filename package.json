{
  "name": "zhanshi",
  "private": true,
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html",
    "deploy": ".\\ossutil.exe cp -r . oss://yhy0624/ -f --exclude .git --exclude node_modules --exclude ossutil --exclude package-lock.json --exclude .ossutilconfig --exclude zhanshi_backup --exclude bun.lock"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
